services:
  backend:
    build:
      context: ./Backend
    ports:
      - "5000:8080"

  frontend:
    build:
      context: ./Frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend